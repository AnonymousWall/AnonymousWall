<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="warn">
    <Properties>
        <Property name="log.pattern">%d{ISO8601} [%t] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}"/>
        </Console>
    </Appenders>

    <Loggers>
        <Logger name="io.micronaut.runtime" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="io.micronaut.cache" level="${env:CACHE_LOG_LEVEL:-debug}" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="io.micronaut.cache.interceptor" level="${env:CACHE_LOG_LEVEL:-debug}" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="com.anonymous.wall" level="${env:APP_LOG_LEVEL:-debug}" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="io.micronaut.http" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="io.micronaut.data" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="org.hibernate" level="warn" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="warn">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
