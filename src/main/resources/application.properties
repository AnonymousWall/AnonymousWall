# Application Configuration - Production Ready
# Use environment variables for sensitive data in production
# Or use application-dev.properties for local development with hardcoded values

# Logging Configuration - can be overridden via LOG_DIR environment variable
LOG_DIR=${LOG_DIR:./logs}

micronaut.security.token.jwt.signatures.secret.generator.secret=${JWT_GENERATOR_SIGNATURE_SECRET:pleaseChangeThisSecretForANewOne}
liquibase.datasources.default.change-log=classpath:db/liquibase-changelog.xml
datasources.default.db-type=${DB_TYPE:mysql}
datasources.default.dialect=${DB_DIALECT:MYSQL}
micronaut.application.name=anonymouswall
micronaut.security.authentication=bearer
datasources.default.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}
datasources.default.url=${DATABASE_URL:jdbc:mysql://localhost:3306/anonymous_wall}
datasources.default.username=${DATABASE_USER:root}
datasources.default.password=${DATABASE_PASSWORD:}
redis.uri=${REDIS_URI:redis://localhost:6379}
micronaut.testresources.enabled=false

context-path=/

# Health endpoint - publicly accessible for load balancer health checks
endpoints.health.enabled=true
endpoints.health.sensitive=false

# Cache Configuration - Production-grade caching with Caffeine
# Cache naming convention: module:business:params

# User cache - frequently accessed user data
micronaut.caches.user:findById.maximum-size=1000
micronaut.caches.user:findById.expire-after-write=5m
micronaut.caches.user:findById.expire-after-access=3m

micronaut.caches.user:findByEmail.maximum-size=1000
micronaut.caches.user:findByEmail.expire-after-write=5m
micronaut.caches.user:findByEmail.expire-after-access=3m

# Post cache - frequently accessed posts
micronaut.caches.post:findById.maximum-size=5000
micronaut.caches.post:findById.expire-after-write=3m
micronaut.caches.post:findById.expire-after-access=2m

# Email verification code cache - short-lived for security
micronaut.caches.auth:emailCode.maximum-size=500
micronaut.caches.auth:emailCode.expire-after-write=15m
micronaut.caches.auth:emailCode.expire-after-access=15m

# Post likes cache - for batch enrichment optimization
micronaut.caches.post:userLikes.maximum-size=10000
micronaut.caches.post:userLikes.expire-after-write=2m
micronaut.caches.post:userLikes.expire-after-access=1m
